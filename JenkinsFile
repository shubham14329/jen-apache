pipeline{
    agent any 
    
    stages{
        stage("init"){
            steps{
                sh """
		sudo cd /var/www/html 
		sudo git pull 
                """
            }
        }
    }
}
